<view class="container">
  <view class="goods">
    <view class="good" wx:for="{{goods}}" wx:key="unique" >
      <image  bindtap="previewImage"  data-src="{{item}}" class="image" src='{{item}}'  > </image>
    </view>
  </view>
</view>